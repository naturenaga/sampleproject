pipeline {
    agent any
    environment {
            PROFILE = "Hudson"
    }

    stages {

        stage('build docker image') {
            steps {
                sh "ls"
            }
        }
     
    }
}
